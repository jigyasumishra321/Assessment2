apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hl-charts.fullname" . }}-test-connection"
  labels:
    {{- include "hl-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hl-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
