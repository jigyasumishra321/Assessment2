apiVersion: v1
kind: Service
metadata:
  name: {{ include "hl-charts.fullname" . }}
  labels:
    {{- include "hl-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hl-charts.selectorLabels" . | nindent 4 }}
